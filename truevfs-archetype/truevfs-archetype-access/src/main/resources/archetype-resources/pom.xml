<?xml version="1.0" encoding="UTF-8"?>
<project
    xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>${groupId}</groupId>
    <artifactId>${artifactId}</artifactId>
    <version>${version}</version>

    <name>${project.artifactId}</name>
    <description>TrueVFS Client Application Demo</description>

    <properties>
        <maven.compiler.showDeprecation>true</maven.compiler.showDeprecation>
        <maven.compiler.source>1.7</maven.compiler.source>
        <maven.compiler.target>1.7</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <scala.version>${scala-version}</scala.version>
        <truevfs.version>${truevfs-version}</truevfs.version>
    </properties>

    <dependencies>
        <!-- A TrueVFS application has a compile time dependency on the module
             TrueVFS Access.
             -->
        <dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-access</artifactId>
            <version>${truevfs.version}</version>
        </dependency>

        <!-- Extension module for monitoring and management with JMX and
             java.util.logging.
             This module is designed for testing purposes only.
             It should not be used in production environments because of its
             negative performance impact.
             -->
        <!--dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-extension-jmx-jul</artifactId>
            <version>${truevfs.version}</version>
            <scope>runtime</scope>
        </dependency-->

        <!-- This should normally be only a runtime dependency in order to
             prompt the user for passwords when accessing WinZip AES encrypted
             ZIP entries or RAES encrypted ZIP files.
             However, declaring it as a compile time dependency allows the
             application to set the class for visual and audible feedback when
             entering wrong passwords. -->
        <dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-key</artifactId>
            <version>${truevfs.version}</version>
        </dependency>

        <!-- A TrueVFS application typically has a dependency on one or more
             file system driver modules for accessing their respective file
             system types.
             When using the client APIs, file system drivers get automatically
             enumerated on the class path at run time by a service locator
             singleton, so adding a run time dependency is sufficient for
             accessing the respective file system types.
             These sample applications have some compile time dependencies
             however because they directly configure and inject some drivers.
             -->

        <!-- This driver provides access to the following URI schemes:
             file: platform file system.
             This dependency is already added by the modules TrueVFS Access File*.
             -->
        <!--dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-driver-file</artifactId>
            <version>${truevfs.version}</version>
            <scope>runtime</scope>
        </dependency-->

        <!-- This driver provides access to the following URI schemes:
             http|https: Hyper Text Transport Protocol.
             You cannot access these schemes via the TrueVFS Access File* API,
             you need to use the TrueVFS Access Path API instead.
             -->
        <dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-driver-http</artifactId>
            <version>${truevfs.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- This driver provides access to the following URI schemes and
             archive file extensions:
             tar: TAR file format.
             tgz|tar.gz: GZIP compressed TAR file format.
             tb2|tbz|tar.bz2: BZIP2 compressed TAR file format.
             -->
        <dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-driver-tar</artifactId>
            <version>${truevfs.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- This driver provides access to the following URI schemes and
             archive file extensions:
             tzp|zip.rae|zip.raes: RAES encrypted ZIP file format.
             -->
        <dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-driver-tzp</artifactId>
            <version>${truevfs.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- This driver provides access to the following URI schemes and
             archive file extensions:
             zip: ZIP file format.
             ear|jar|war: JAR file format.
             odt|ott|odg|otg|odp|otp|ods|ots|odc|otc|odi|oti|odf|otf|odm|oth|odb: Open Document File format.
             exe: Self extracting ZIP file format.
             -->
        <dependency>
            <groupId>de.schlichtherle.truevfs</groupId>
            <artifactId>truevfs-driver-zip</artifactId>
            <version>${truevfs.version}</version>
            <!--scope>runtime</scope-->
        </dependency>

        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-library</artifactId>
            <version>${scala.version}</version>
        </dependency>

        <!-- This is only required for the Scala compiler.
             If you are not using Scala in your project, then you can remove
             this dependency. -->
        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>annotations</artifactId>
            <version>2.0.0</version>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
                <version>3.0.1</version>
                <executions>
                    <execution>
                        <id>scala-compile-first</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>add-source</goal>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>scala-test-compile</id>
                        <phase>process-test-resources</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <!-- For NetBeans. -->
                    <showDeprecation>${maven.compiler.showDeprecation}</showDeprecation>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
