    -----------------------
    Getting started without Maven
    -----------------------
    Christian Schlichtherle
    -----------------------

Getting started without Maven

    If you do not want to use Maven then you can easily
    {{{http://search.maven.org/#search%7Cga%7C1%7Cg%3A%22de.schlichtherle.truevfs%22%20AND%20v%3A%22${project.version}%22}<<download individual TrueVFS artifacts>>}}
    from Maven Central.
    You can also directly {{{http://repo1.maven.org/maven2/de/schlichtherle/truevfs/truevfs-samples/${project.version}/truevfs-samples-${project.version}-jar-with-dependencies.jar}<<download the all-in-one JAR>>}}.

    The all-in-one JAR saves you from grabbing the module JARs individually.
    However, as the name implies, it includes all sample code which bloats the
    code size of the application.
    More importantly, this will also initially set up the TrueVFS Access File* API to
    detect all canonical file extensions of all supported archive file types
    in TrueVFS ${project.version} - see the table at the end of this page.
    This may produce some unwanted side effects on an unintended access to an
    archive file:

    * The virtual file system of an archive file is automatically mounted.

        * This may confuse the application because the archive file
          unexpectedly behaves like a directory instead of a file.

	* In case of TAR files, this can be considerably slow because this file
          format doesn't provide a central directory and thus the driver will
          unpack the entire TAR file into a temporary directory first.

        * In case of a plain EXE file, an attempt is made to locate the central
          directory of a self extracting ZIP file which will cause the entire
          file to be read.

    * A Swing or console based dialog prompts the user for a password to access
      a RAES encrypted ZIP file.

    []

    To avoid these unwanted side effects, you should <always> configure the
    TrueVFS Access File* API to detect exactly only the file suffixes of the archive
    types required to access.
    {{{../usecases/aff.html}Here}} is a sample available for this task.
    For further information, please refer to the Javadoc for the class
    {{{../apidocs/de/schlichtherle/truevfs/file/TArchiveDetector.html}<<<TArchiveDetector>>>}}.

* Initial Setup

    Following are the canonical archive file extensions which will be detected
    by an application using the TrueVFS Access File* API according to the initial set
    up of the all-in-one JAR <<<truevfs-samples-${project.version}-jar-with-dependencies.jar>>>:

*--*--*
||Canonical File Extensions||File System Driver Class|
*--+--+
|<<<ear>>>, <<<jar>>>, <<<war>>>|{{{../apidocs/net/truevfs/driver/zip/JarDriver.html}<<<JarDriver>>>}}|
*--+--+
|<<<exe>>>|{{{../apidocs/net/truevfs/driver/zip/ReadOnlySfxDriver.html}<<<ReadOnlySfxDriver>>>}}|
*--+--+
|<<<odt>>>, <<<ott>>>, <<<odg>>>, <<<otg>>>, <<<odp>>>, <<<otp>>>, <<<ods>>>, <<<ots>>>, <<<odc>>>, <<<otc>>>, <<<odi>>>, <<<oti>>>, <<<odf>>>, <<<otf>>>, <<<odm>>>, <<<oth>>>, <<<odb>>>|{{{./apidocs/net/truevfs/driver/zip/OdfDriver.html}<<<OdfDriver>>>}}|
*--+--+
|<<<tar>>>|{{{../apidocs/net/truevfs/driver/tar/TarDriver.html}<<<TarDriver>>>}}|
*--+--+
|<<<tar.bz2>>>, <<<tb2>>>, <<<tbz>>>|{{{../apidocs/net/truevfs/driver/tar/TarBZip2Driver.html}<<<TarBZip2Driver>>>}}|
*--+--+
|<<<tar.gz>>>, <<<tgz>>>|{{{../apidocs/net/truevfs/driver/tar/TarGZipDriver.html}<<<TarGZipDriver>>>}}|
*--+--+
|<<<tzp>>>, <<<zip.rae>>>, <<<zip.raes>>>|{{{../apidocs/net/truevfs/driver/zip/raes/SafeZipRaesDriver.html}<<<SafeZipRaesDriver>>>}}|
*--+--+
|<<<zip>>>|{{{../apidocs/net/truevfs/driver/zip/ZipDriver.html}<<<ZipDriver>>>}}|
*--+--+
